const Schema = require('../lib/Schema');

describe('Schema', () => {

  // add a test schema
  const str = 'stuff';
  const bool = false;
  const num = 2;
  const date = new Date();

  it.skip('validates a correct model', () => {
  });
  
  it.skip('throws on invalid model', () => {
  });

  // more test cases...
});